import { useState, useEffect } from 'react';

const MAX_PHOTOS = 10;
const useRandom = (): any => {
	const [data, setData] = useState([]);
	for (let i = 0; (i = MAX_PHOTOS); i++) {
		const;
	}
	useEffect(() => {
		getPhotos();
	}, []);
};
